[project]
name = "cinegraph"
version = "0.1.0"
description = "Spoiler-aware, self-hosted intelligence for episodic media"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.141.1",
    "fastembed==0.8.0",
    "httpx>=0.28.1",
    "langchain==1.3.14",
    "langchain-openai>=1.3.5",
    "langgraph>=1.2.9",
    "openai>=2.45.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.2",
    "pypdf>=6.15.0",
    "pytest-bdd>=8.1.0",
    "qdrant-client==1.19.0",
    "rapidfuzz>=3.14.5",
    "uvicorn>=0.52.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cinegraph"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests", "scripts"]
extend-exclude = ["knowledge"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.coverage.run]
branch = true
source = ["src/cinegraph"]

[tool.coverage.report]
fail_under = 87
show_missing = true

[tool.coverage.xml]
output = "coverage.xml"

[tool.coverage.json]
output = "coverage.json"

[tool.mypy]
python_version = "3.12"
files = [
    "src/cinegraph/domain",
    "src/cinegraph/ports",
    "src/cinegraph/config",
    "src/cinegraph/application/models",
    "src/cinegraph/application/policy",
    "src/cinegraph/application/serialization",
    "src/cinegraph/application/service",
]
check_untyped_defs = true
disallow_untyped_defs = true
warn_redundant_casts = true
warn_unused_ignores = true
no_implicit_optional = true

[dependency-groups]
dev = [
    "mypy>=1.17,<1.19",
    "pre-commit>=4.3,<5",
    "pytest-cov>=6.2,<7",
    "ruff>=0.12.10,<0.13",
]
