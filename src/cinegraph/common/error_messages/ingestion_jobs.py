class IngestionJobErrorMessages:
    SERIES_ID_REQUIRED = "Ingestion job series_id is required."
    EPISODE_REQUIRES_SEASON = "An ingestion job episode scope requires a season number."
    UTC_TIMESTAMP_REQUIRED = "Ingestion job timestamps must be timezone-aware UTC values."
    MAX_ATTEMPTS_INVALID = "Ingestion job max_attempts must be within the configured bounds."
    ATTEMPTS_INVALID = "Ingestion job attempts cannot be negative or exceed max_attempts."
    LEASE_ONLY_RUNNING = "A lease owner and expiry are valid only while a job is running."
    TERMINAL_JOB_IMMUTABLE = "A terminal ingestion job cannot be reclaimed or transitioned."
    WORKER_REQUIRED = "A non-empty opaque worker identifier is required."
    STALE_LEASE = "The worker lease is no longer valid for this ingestion job."
    INVALID_TRANSITION = "The requested ingestion job transition is not valid."
    SOURCE_FINGERPRINT_REQUIRED = "Ingestion job source fingerprint is required."
    PIPELINE_REVISION_REQUIRED = "Ingestion job pipeline revision is required."
    ERROR_CODE_NOT_ALLOWLISTED = "Only an allowlisted stable ingestion error code may be stored."
    CONFIGURATION_INVALID = "Ingestion job configuration values are invalid."
    INVENTORY_OUTPUT_MUST_BE_UNDER_ROOT = "Inventory output must be beneath the corpus root."
